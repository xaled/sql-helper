#!/usr/bin/python
<%!
    from profiles.filters import upper,upperFirst,lower
%>
<%
    constructargs = "%s=None"%(vars[0]['name'].lower())
    for var in vars[1:]:
        constructargs  += "%s=None"%(vars[0]['name'].lower())
%>



class ${classname}
    """
     ${classname} generated by sql-helper for database ${dbname} version ${dbversion}
     package: ${package}
      https://github.com/xaled/sql-helper.git
    """


    # constructor
    def __init__(self, ${constructargs}):
        self.id = -1
% for var in vars:
        self.${var['name']|lower} = ${var['name']|lower}
% endfor

